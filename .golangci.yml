version: 2

run:
  timeout: 5m
  skip-dirs:
    - old
    - resources
  skip-files:
    - "old/.*\\.go$"

linters:
  enable:
    - unused
    - ineffassign
    - unparam
    - staticcheck

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
